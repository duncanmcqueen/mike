; npm 12+ blocks non-registry tarball dependencies by default.
; The backend depends on xlsx from cdn.sheetjs.com (a direct dependency),
; so allow remote tarballs for root-level dependencies only.
allow-remote=root
