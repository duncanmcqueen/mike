# Optional. In development, the HTTPS add-in server proxies /api to the
# local backend, avoiding mixed-content errors inside Word.
# API_BASE_URL=https://mike.example.com/api

# Optional URL for links that open the full MikeOSS web application.
MIKE_WEB_URL=http://localhost:3000
